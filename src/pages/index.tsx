import Navbar from '@/components/Navbar';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div className='min-h-screen'>
      <Navbar />
    </div>
  );
};

export default Home;
